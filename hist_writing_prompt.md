# 一、核心原则

- **主要压缩对象**：多轮tool calls及其返回结果。占据大量 token，但只有少数信息对后续决策有用。
- **核心原则**：适当丢掉细节，保留决策、结论、状态变更、依赖关系。
- **保留阈值**：任何可能导致后续行为错误的信息不能丢失（例如：一次 API 返回的错误码、一个已分配的资源 ID、一个状态变量的最终值）。

# 二、文件格式规范

文件必须严格按照以下结构组织：

```markdown
# 压缩对话历史

## 时间线摘要
按时间顺序，每轮关键交互(只保留关键交互，冗长、多轮的工具调用loop必须压缩成简短内容)用一句话总结，添加在之前的时间线部分后面，过久的时间线适当压缩（不要完全删除！！！）：

- [序号] 用户提出需求：……
- [序号] Agent 调用工具 X，目的：……
- [序号] 工具返回结果摘要：……
- [序号] Agent 根据结果作出决策/下一步动作：……

## 状态变更记录（重要）
记录所有被修改的持久状态：
- 环境变量：`DEBUG=true` 已设置
- 文件系统：`/tmp/output.json` 已写入，内容包含字段 `status: "ok"`
- 内存状态：用户的 current_page 变更为 “dashboard”
- 其他：锁、信号量、定时器等